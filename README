INTRODUCTION

Mpifold provides parallel iterators using MPI. It is implemented as a
functor taking a Foldable module and returning a module with
parallelised iterators.


USAGE

For a simple example of usage see example.ml.
